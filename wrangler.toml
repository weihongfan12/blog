# Cloudflare Pages 配置文件
# 这个文件告诉 Cloudflare Pages 如何构建和部署你的网站

# 构建命令（静态网站不需要构建）
# build_command = "npm run build"

# 输出目录（静态网站直接使用根目录）
# output_directory = "."

# 环境变量
[env.production]
  NODE_ENV = "production"